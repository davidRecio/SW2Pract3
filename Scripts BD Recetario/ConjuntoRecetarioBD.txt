create table conjunto_recetario( 
	nombre_conjunto varchar (20) primary key not null,
	nombre_recetario varchar (20),
	usuario_id integer not null,
	foreign key (nombre) references recetario(nombre),
	foreign key (usuario_id) references usuario(usuario_id)
);

